spring.datasource.url=jdbc:postgresql://localhost:${spring.datasource.database.port}/${spring.datasource.database.name}
spring.datasource.username=test
spring.datasource.password=test

spring.datasource.database.port=5432
spring.datasource.database.name=test

spring.flyway.enabled=true
spring.flyway.clean-mode=always
spring.flyway.url=${spring.datasource.url}
spring.flyway.user=${spring.datasource.username}
spring.flyway.password=${spring.datasource.password}

mail-service.send-by-time.url=URI